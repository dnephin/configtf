machine:
  pre: ['curl -sSL http://bit.do/circle-docker | bash -s -- 1.10.0']
  services: [docker]
  environment: {DOCKER_API_VERSION: '1.22'}

dependencies:
  cache_directories: [vendor]
  override: ['curl -sSL -o ~/dobi http://bit.do/dobi-0-7; chmod +x ~/dobi']

test:
  override: [~/dobi deps all]
